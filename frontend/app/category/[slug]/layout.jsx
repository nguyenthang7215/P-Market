// Đường dẫn: frontend/app/category/[slug]/layout.jsx
import HomeLayout from '../../home/layout'; // <-- Import từ app/home/layout.jsx

// export async function generateMetadata({ params }) {
//   return { title: `Danh mục ${params.slug} - P-Market` };
// }

export default HomeLayout; // Chỉ cần export lại HomeLayout